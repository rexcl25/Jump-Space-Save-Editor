# .NET build output - regenerated by `dotnet build`, no need to track it.
# (This project also flattens its release output into build\, see the
# OutputPath setting in JumpSpaceEditor.csproj.)
bin/
obj/
build/

# IDE folders
.vs/
.idea/
*.user

# OS junk
.DS_Store
Thumbs.db
